# Frontend Environment Variables
# Copy to .env.local and update with your backend URL

# Development (runs backend locally on port 8000)
VITE_API_URL=http://localhost:8000

# Production (set this in Vercel dashboard)
# VITE_API_URL=https://opportunity-radar-api.onrender.com
